package com.gamerbah.gamezonesmp.command;
/* Created by GamerBah on 2/5/21 */

public class ProfileCommand {} /*implements CommandExecutor {

	private SMP plugin;

	public ProfileCommand(SMP plugin) {
		this.plugin = plugin;
	}

	@Override
	public boolean onCommand(CommandSender sender, Command command, String label, String[] args) {
		if (!(sender instanceof Player)) {
			return true;
		}

		Player player = (Player) sender;
		new ProfileMenu(plugin, player).build(player).open();
		EventSound.playSound(player, EventSound.INVENTORY_OPEN);

		return false;
	}

}*/
